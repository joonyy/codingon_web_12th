<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <script src="https://unpkg.com/axios/dist/axios.min.js"></script>
  <title>axios post 실습</title>
</head>
<body>
  <h1>axios post 로그인 </h1>
  <p>(참고) app.js에 저장한 아이디는 홍길동, 비밀번호는 1234, 아이디와 비밀번호가 일치할 때와 불일치할 때 어떻게 다른지 확인해보세요!! </p>
  
  <form name="loginForm">
    <label for="id">아이디 : </label>
    <input type="text" name="id" id="id" placeholder="아이디 입력" required>
    <label for="pw">비밀번호 : </label>
    <input type="password" name="pw" id="pw" placeholder="비밀번호 입력" required>
    <button type="button" onclick="login()"> 로그인 </button>
  </form>
  <hr>
  <div class="result"></div>
  <script>
    function login(){
      const form = document.forms['loginForm'];
      if(form.id.value==='' || form.pw.value === '') alert('아이디와 패스워드는 모두 입력하셔야 합니다');
      else{
        const data = {
          userId : form.id.value,
          userPw : form.pw.value
        }
        axios({
          method:"POST",
          url:'/axiosPost',
          data:data,
        }).then(res =>{
          result= document.querySelector('.result');
          result.textContent = `${res.data}`;
        })
      }
  }
  </script>
</body>
</html>